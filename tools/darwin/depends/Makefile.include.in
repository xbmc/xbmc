SDK=@use_sdk@
ARCH=@use_arch@
DARWIN=@use_darwin@
PREFIX=@use_prefix@
STAGING=@use_staging@

# hack, include both possible paths which
# depends on where we do the make.
-include Makefile.buildtools
-include ../Makefile.buildtools
