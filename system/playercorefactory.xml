<playercorefactory>
  <players>
    <!-- These are compiled-in as re-ordering them would break scripts
    The following aliases may also be used:
      audiodefaultplayer, videodefaultplayer
    <player name="DVDPlayer" audio="true" video="true" />
    <player name="MPlayer" audio="true" video="true" />
    <player name="PAPlayer" audio="true" />
    -->
  </players>

  <rules>
    <rule protocols="rtv|mms" player="MPlayer" />
    <rule protocols="hdhomerun|rtmp" player="DVDPlayer" />
    <rule protocols="lastfm|shout" player="PAPlayer" />

    <!-- dvdplayer can play standard rtsp streams -->
    <rule protocols="rtsp" filetypes="!(rm|ra)"  player="PAPlayer" />

    <!-- Internet streams -->
    <rule internetstream="true">
      <rule mimetypes="video/x-flv|video-flv|audio/aacp|application/sdp" player="DVDPlayer" />
      <rule mimetypes="application/octet-stream" filetypes="mp2" player="PAPlayer" />
      <rule filetypes="wmv" player="MPlayer" />
    </rule>

    <!-- DVDs -->
    <rule dvd="true" player="DVDPlayer" />
    <rule dvdfile="true" player="DVDPlayer" />
    <rule dvdimage="true" player="DVDPlayer" />

    <!-- Only mplayer can handle these properly -->
    <rule filetypes="asf" player="MPlayer" />

    <!-- dvdplayer generally plays these better -->
    <rule filetypes="mp4" player="DVDPlayer" />
  </rules>
</playercorefactory>
