aux_source_directory(. windowing_SRCS)
if(APPLE)
	aux_source_directory(osx windowing_os_SRCS)
	list(REMOVE_ITEM windowing_os_SRCS osx/WinEventsIOS.mm)
	list(REMOVE_ITEM windowing_os_SRCS osx/WinSystemIOS.mm)
elseif(UNIX)
	aux_source_directory(X11 windowing_os_SRCS)
else(APPLE)
	aux_source_directory(windows windowing_os_SRCS)
endif(APPLE)
include_directories(. ../input)
add_sources(${windowing_SRCS} ${windowing_os_SRCS})
