INCLUDES=-I.
CFLAGS=-fPIC
SYSDIR=../../../system
SO=librefmem.so
SLIB=$(SYSDIR)/$(SO)

SRCS=alloc.c debug.c

$(SLIB): $(SRCS)
	$(CC) $(CFLAGS) $(INCLUDES) $(SRCS) -shared -o $(SLIB)
	
-include ../../../Makefile.include
