INCLUDES=-I.
ARCH=@ARCH@
SO=RefMem-$(ARCH).so
ARCHIVE=$(SO:.so=.a)
SYSDIR=../../../system
SLIB=$(SYSDIR)/$(SO)

#DIRS=../librefmem
OBJS+=alloc.o
OBJS+=debug.o

.PHONY: compile

$(SLIB)
	$(CXX) $(CFLAGS) -shared -o $(SLIB)
	
-include ../../../Makefile.include
