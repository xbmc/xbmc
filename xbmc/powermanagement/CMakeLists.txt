aux_source_directory(. powermanagement_SRCS)
if(APPLE)
	aux_source_directory(osx pwr_os_SRCS)
elseif(UNIX)
	aux_source_directory(linux pwr_os_SRCS)
else()
	aux_source_directory(windows pwr_os_SRCS)
endif()
add_sources(${powermanagement_SRCS} ${pwr_os_SRCS})
