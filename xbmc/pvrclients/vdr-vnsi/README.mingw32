Cross-compiling the VNSI addon from Linux with MINGW32
-------------------------------------------------------

Install the MINGW32 cross-compiler:

# apt-get install mingw32

Compile the addon:

# make -f Makefile.mingw

The resulting addon will be:

addons/pvr.vdr.vnsi/XBMC_VDR_vnsi_WIN32.pvr
