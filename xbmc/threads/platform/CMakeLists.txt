if(UNIX)
	aux_source_directory(pthreads threads_os_SRCS)
else()
	aux_source_directory(win threads_os_SRCS)
endif()
aux_source_directory(. threads_platform_SRCS)
add_library(threads_platform STATIC ${threads_platform_SRCS} ${threads_os_SRCS})