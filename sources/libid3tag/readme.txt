libid3tag 0.15.1.b
zlib 1.2.3

XBMC changes
============

Changed compat.c (via compat.gpref) to support the "PRI " tag that WMP adds
for it's track level data (maps to PRIV).

Also, has support for fixing iTunes broken v2.4 tags (APIC in particular)
